<ui:FluentWindow x:Class="WpfHarness.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:views="clr-namespace:WpfHarness.Views"
                 Title="Wpf Harness"
                 Width="1200" Height="800" MinWidth="980" MinHeight="640"
                 UseLayoutRounding="True" SnapsToDevicePixels="True"
                 FontFamily="Segoe UI, Microsoft YaHei UI"
                 ExtendsContentIntoTitleBar="True"
                 Background="{DynamicResource WindowBgBrush}"
                 WindowStartupLocation="CenterScreen">

    <ui:FluentWindow.Resources>
        <!-- 侧栏条目 -->
        <Style x:Key="SidebarItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="8"
                                BorderThickness="2,0,0,0" BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CardHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceBrush}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 任务条目删除按钮：hover 时才出现 -->
        <Style x:Key="HoverDeleteButton" TargetType="Button">
            <Setter Property="Opacity" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextTertiaryBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ElevatedBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                    <Setter Property="Opacity" Value="1" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ui:FluentWindow.Resources>

    <ui:FluentWindow.InputBindings>
        <KeyBinding Modifiers="Control" Key="N" Command="{Binding NewTaskCommand}" />
    </ui:FluentWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Wpf Harness" Grid.Row="0" ShowMaximize="True" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="272" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!-- ============ 侧栏 ============ -->
            <Border Grid.Column="0" Background="{DynamicResource SidebarBrush}"
                    BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,0,1,0">
                <Grid Margin="10,4,10,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 品牌行 -->
                    <Grid Grid.Row="0" Margin="6,6,6,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="26" Height="26" CornerRadius="7"
                                Background="{DynamicResource AccentSubtleBrush}"
                                BorderBrush="{DynamicResource AccentBorderBrush}" BorderThickness="1">
                            <TextBlock Text="⚡" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Grid.Column="1" Text="Wpf Harness" Margin="9,0,0,0" FontSize="13.5"
                                   FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"
                                   VerticalAlignment="Center" />
                        <Border Grid.Column="2" Style="{StaticResource Badge}">
                            <TextBlock Text="MVP" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}" />
                        </Border>
                    </Grid>

                    <!-- 新建任务 -->
                    <Button Grid.Row="1" Command="{Binding NewTaskCommand}" Cursor="Hand" Margin="0,0,0,8"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" Background="{DynamicResource AccentBrush}" CornerRadius="9"
                                        Padding="11,0" Height="34">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="＋" Foreground="White" FontSize="13" FontWeight="Bold" />
                                            <TextBlock Text="新建任务" Margin="7,0,0,0" Foreground="White"
                                                       FontSize="13" FontWeight="SemiBold" />
                                        </StackPanel>
                                        <Border Grid.Column="1" Style="{StaticResource Keycap}"
                                                Background="{DynamicResource AccentOverlayBrush}"
                                                BorderBrush="{DynamicResource AccentOverlayBorderBrush}">
                                            <TextBlock Text="Ctrl N" FontSize="10"
                                                       Foreground="{DynamicResource AccentOverlayTextBrush}" />
                                        </Border>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- 搜索（占位） -->
                    <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1"
                            CornerRadius="9" Height="34" Margin="0,0,0,4" Opacity="0.7">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="🔍" FontSize="11" Margin="11,0,0,0" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="搜索任务" Margin="8,0,0,0" FontSize="12"
                                       Foreground="{DynamicResource TextTertiaryBrush}" VerticalAlignment="Center" />
                            <Border Grid.Column="2" Style="{StaticResource Keycap}" Margin="0,0,9,0">
                                <TextBlock Text="Ctrl K" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}" />
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 次级导航（占位） -->
                    <StackPanel Grid.Row="3" Margin="0,4,0,0">
                        <Button Style="{StaticResource GhostButton}" Tag="8" IsEnabled="False" Opacity="0.5" Height="30" HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal" Margin="11,0,0,0">
                                <TextBlock Text="📅" FontSize="12" VerticalAlignment="Center" />
                                <TextBlock Text="自动化" Margin="9,0,0,0" FontSize="12.5" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource GhostButton}" Tag="8" IsEnabled="False" Opacity="0.5" Height="30" HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal" Margin="11,0,0,0">
                                <TextBlock Text="🧩" FontSize="12" VerticalAlignment="Center" />
                                <TextBlock Text="插件市场" Margin="9,0,0,0" FontSize="12.5" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- 任务列表 -->
                    <Grid Grid.Row="4" Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="10,0,10,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="任务" FontSize="11" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextTertiaryBrush}" />
                            <TextBlock Grid.Column="1" FontSize="11"
                                       Foreground="{DynamicResource TextTertiaryBrush}"
                                       Text="{Binding Sessions.Count, Mode=OneWay}" />
                        </Grid>
                        <ListBox Grid.Row="1" ItemsSource="{Binding Sessions}"
                                 SelectedItem="{Binding SelectedSession}"
                                 ItemContainerStyle="{StaticResource SidebarItemStyle}"
                                 Background="Transparent" BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"
                                                       Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12.5" />
                                            <TextBlock Text="{Binding UpdatedAt, Converter={StaticResource RelativeTime}}"
                                                       FontSize="10.5" Margin="0,1,0,0"
                                                       Foreground="{DynamicResource TextTertiaryBrush}" />
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="✕" FontSize="10" Width="20" Height="20"
                                                Style="{StaticResource HoverDeleteButton}"
                                                Command="{Binding DataContext.DeleteSessionCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                                CommandParameter="{Binding}" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>

                    <!-- 底部用户区 -->
                    <Border Grid.Row="5" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,1,0,0"
                            Padding="8,10,4,2" Margin="0,8,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                                    Background="{DynamicResource AccentBrush}" VerticalAlignment="Center">
                                <TextBlock Text="H" Foreground="White" FontSize="12" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="harness-user" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" />
                                <TextBlock Text="本地 · v0.1" FontSize="10.5" Foreground="{DynamicResource TextTertiaryBrush}" />
                            </StackPanel>
                            <Button Grid.Column="2" Style="{StaticResource GhostButton}" Tag="7" Content="⚙" FontSize="14"
                                    Width="28" Height="28" ToolTip="设置"
                                    Command="{Binding OpenSettingsCommand}" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- ============ 主区域 ============ -->
            <Grid Grid.Column="1">
                <views:HomeView Visibility="{Binding HasSession, Converter={StaticResource InverseBoolToVis}}" />
                <views:ChatView Visibility="{Binding HasSession, Converter={StaticResource BoolToVis}}" />
            </Grid>
        </Grid>

        <ui:ContentDialogHost x:Name="RootContentDialog" Grid.Row="0" Grid.RowSpan="2" />
    </Grid>
</ui:FluentWindow>
